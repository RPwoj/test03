{% extends 'base.html.twig' %}

{% block title %}Hello HomePageController!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
  
{% if app.user %}
    {{ form(form) }}
{% endif %}


<div class="cards-holder">
    {% for user in users %}

        <a href="{{ path('app_profile', {id: user.id}) }}">
            <div class="card">
                {{ user.username|e }}
                <img height="50" width="50" src="{{ asset('uploads/avatars/' ~ user.avatar) }}" alt="avatar" />
            </div>
        </a>
    {% endfor %}
</div>
{% for post in posty %}
    {{post.title}}
    {{post.description}}
{% endfor %}


<ul>
    {# {% for animal in animals %} #}
            {# <li>{{animal.owner}}</li> #}
    {# {% endfor %} #}
</ul>
    This friendly message is coming from:
    <ul>
        <li>Your controller at <code><a href="{{ 'C:/xampp/htdocs/test03/src/Controller/HomePageController.php'|file_link(0) }}">src/Controller/HomePageController.php</a></code></li>
        <li>Your template at <code><a href="{{ 'C:/xampp/htdocs/test03/templates/home_page/index.html.twig'|file_link(0) }}">templates/home_page/index.html.twig</a></code></li>
    </ul>
</div>
{% endblock %}
